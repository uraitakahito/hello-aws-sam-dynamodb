# ローカル開発用環境変数のサンプル。
# 実際には sam local が AWS_SAM_LOCAL=true を自動で注入するため、
# 通常このファイルを .env にコピーする必要はない。
# AWS CLIでDynamoDB Localを操作する scripts/create-local-table.sh 用。

AWS_REGION=ap-northeast-1
DYNAMODB_ENDPOINT=http://localhost:8000
TODOS_TABLE_NAME=Todos
